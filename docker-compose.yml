services:
  db:
    image: postgres:15-alpine
    container_name: postgres_db
    restart: always
    env_file:
      - .env  # <-- Докер возьмет пароли отсюда
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    container_name: django_backend
    restart: always
    env_file:
      - .env  # <-- Django возьмет секреты отсюда
    volumes:
      - ./backend:/app  # Чтобы изменения в коде сразу применялись без перезапуска
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=True  # Это не секрет, это флаг-переключатель, оставим тут
      - POSTGRES_HOST=db
    depends_on:
      - db
    command: >
      sh -c "python manage.py collectstatic --noinput &&
             python manage.py migrate && 
             python manage.py createsuperuser --username admin --email admin@example.com --noinput || true &&
             gunicorn source.wsgi:application --bind 0.0.0.0:8000 --workers 3"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nextjs_frontend
    # Порт 3000 можно даже не прокидывать наружу, Nginx сам с ним общается внутри сети,
    # но для удобства отладки оставим:
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost/api/v1
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    container_name: nginx_server
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data:
  static_volume:
  media_volume: